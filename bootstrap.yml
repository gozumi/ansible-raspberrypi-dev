- hosts: localhost
  become: yes

  tasks:
    - name: Update packages
      ansible.builtin.apt:
        update_cache: "yes"
        upgrade: "yes"
        state: latest

    - name: Install packages
      ansible.builtin.apt:
        name:
          - bat
          - zsh
